This Plot2 code is also fairly simple:
First the data is read in, next the date and time are variables are changed from characters to objects, next the basic plot function is called, and the plot is annotated with a Title. Finally, a .png image is created for the plot.

