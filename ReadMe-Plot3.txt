This Plot3 code is also fairly simple:
First, the data is read in, next both the data and time as characters are changed into objects. Next, the basic plot function is called and the plot is annotated with a Title. Finally, a .png image is created for the plot.

